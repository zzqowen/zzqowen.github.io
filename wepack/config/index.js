'use strict'
const path = require('path');

module.exports = {
  dev: {
    host: 'localhost',
    port: '8080',
    autoOpenBrowser: false,
    assetsPublicPath: '/'
  },
  build: {
    assetsPublicPath: './'
  }
}